# Public Google Analytics 4 measurement ID, when analytics is configured.
NUXT_PUBLIC_GA_MEASUREMENT_ID=

# Keep deployment and affiliate credentials out of Git.
# Add server-only variables here only when the application requires them.
